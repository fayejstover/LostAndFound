// Header.js
import React from 'react';
import './App.css'; // or './header.css' if you created a separate file

const Header = () => {
  return (
    <header className="header">
      LostAndFound+
    </header>
  );
};

export default Header;